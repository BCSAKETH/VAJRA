node_modules/
build/
dist/
coverage/
.DS_Store
*.log
**/*.log
.env*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.pyc

# TTS audio cache (generated at runtime)
vajra_backend/cache/

# Datasets & Large Files
*.csv
*.pdf
.git/index.lock
.token_cache
**/.token_cache
screenshots/

# Exclude credentials / secrets
Accounts.txt
accounts.txt
*accounts*
*credentials*
*.pem
*.key

# Exclude temporary scratch & test scripts
scratch/
**/scratch/
scratch_*.py
*audit_ledger*.py

# Exclude temporary media recordings
video_full.mp4
*.mp4
*.webp

# Whitelist Critical Backend & App Assets (Never Ignore)
!requirements.txt
!**/requirements.txt
!vajra_backend/*.joblib
!*.ttf
!*.woff2



# Security: never publish secrets or dev scratch (case-insensitive)
[Cc]redentials*
Credentials.txt
**/.token_cache
.token_cache
vajra_backend/scratch/
**/scratch/
vajra_backend_manual_deploy.zip
vajra_client_deploy.zip

# Backup of real numba/llvmlite + local .venv, moved out of vajra_backend/vendor
# during the CLI-deploy size fix (see vajra_backend/vendor/numba/__init__.py) --
# local-only, never meant for git (the local .venv copy alone is 1.4GB).
vendor_backup_numba/
vlogin11.json
